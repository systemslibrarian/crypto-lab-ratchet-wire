{
  "name": "ratchet-wire",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "deploy": "npm run build && gh-pages -d dist",
    "preview": "vite preview",
    "test": "vitest"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "happy-dom": "^20.10.6",
    "typescript": "^5.3.3",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "dependencies": {
    "@noble/curves": "^2.2.0"
  }
}
